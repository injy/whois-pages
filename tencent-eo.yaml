# Tencent Cloud EO (EdgeOne) Configuration
# Deploy this project to Tencent Cloud EdgeOne

runtime: nodejs18

build:
  command: npm run build

start:
  command: cd backend && node server.js

env:
  NODE_ENV: production
  PORT: "3000"
