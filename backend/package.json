{
  "name": "whois-domain-lookup",
  "version": "1.0.0",
  "description": "WHOIS/RDAP domain lookup application",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "cd ../frontend && npm install && npm run build",
    "install:all": "npm install && cd ../frontend && npm install"
  },
  "keywords": ["whois", "rdap", "domain", "lookup"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
